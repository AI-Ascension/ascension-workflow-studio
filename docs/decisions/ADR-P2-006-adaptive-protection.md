# ADR-P2-006: Treat adaptive plans as protected projections

Status: accepted for Phase 2.

An `adaptive_region` is visible with its qualified identity and configuration, but is read-only in the Studio. Planner output, accepted/rejected/superseded layers, and protected action settlement remain harness-owned until a separately admitted owner contract exists.
