# ADR-P2-009: Provide list-editor parity for graph interaction

Status: accepted for Phase 2.

Canvas operations have a keyboard-accessible list editor equivalent. Status uses text and badges in addition to color. Buttons expose labels, protected regions explain their boundary, and the layout collapses to a single-column form on narrow screens.
