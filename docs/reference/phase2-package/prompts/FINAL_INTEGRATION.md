# Final integration and independent acceptance

Integrate only reviewed candidates at exact heads. Refresh Phase-1 artifact pins
and run the full affected compiler, browser, backend, process, fault, security,
accessibility and performance gates. Verify a real strict workflow round-trip,
a dynamic region with two actual accepted plans, durable draft restart, lost-
response command lookup, browser disconnect without cancelling a run, historical
inspection with zero mutations, and no cross-origin/role bypass. Check production
CSP/assets, licenses/SBOM, docs, evidence redaction and linked draft PRs.
Every mandatory requirement needs executed evidence or an explicit unverified/
blocked entry with owner/next step. Report actual model lineage/config separately.
Close descendants/leases/processes you own. Use the final-report template; do not
merge, release, deploy or claim those operations happened. A package check or
mock server cannot satisfy actual Phase-1 integration.
