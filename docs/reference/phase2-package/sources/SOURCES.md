# Sources and evidence limits

Accessed 2026-09-09. This package is an implementation specification, not evidence
that the future Studio or assumed Phase-1 integration has run. URLs and exact
reference-byte hashes are recorded in `source-register.json` and
`reference-provenance.json`. No external implementation source or commercial
example was copied into the product plan.

**P01 — Prior Phase-1 package supplied in this conversation.** The source ZIP
SHA-256 is `e447bc09d82498413dc949adb7d059901c63ca4eab26d629414dd9d65d619566`.
It establishes the intended harness ownership, strict/dynamic runtime, headless
APIs, protected actions and D0-D3 Luna Max hierarchy. Its proposed schemas are
not an authoritative artifact release. Selected files are preserved byte-for-byte.

**G01 — Connected GitHub repository search.** A read-only search for
`org:AI-Ascension ascension-workflow` returned no matches during packaging.
This is not a proof of repository absence or Phase-1 nonimplementation. No
current implementation revision is fabricated; exact source admission belongs
to the executing agent, under the user's Phase-1-complete assumption.

**S01 — React Flow accessibility.** Primary documentation describes keyboard and
screen-reader features. Studio still needs custom-control and whole-journey tests.
**S02 — React Flow save/restore.** Canvas serialization is available; this proposal
keeps it distinct from executable workflow semantics.
**S03 — W3C dragging-movement guidance.** Informs the required non-drag pointer
alternative in addition to keyboard access.
**S04 — Playwright accessibility testing.** Supports automated browser checks and
explicitly notes the need for manual assessment.
**S05 — W3C target-size guidance.** Used for interaction target evaluation.
**S06 — WHATWG server-sent events.** Source for SSE framing and reconnect cursor
semantics; custom fetch streaming requires explicit implementation/testing.
**S07 — OWASP CSRF prevention.** Supports exact source/target-origin checks;
our proposed scoped bearer/pairing design is an original implementation decision.
**S08 — RFC 9110.** Source for strong If-Match conditional-write semantics.
**S09 — OpenAI Luna model page.** Confirms the documented model identifier and
max reasoning option, not the executing account's availability.
**S10 — OpenAI configuration reference.** Describes subagent model/effort and
concurrent-thread controls; it does not prove recursive depth support locally.
**S11 — OpenAI subagents documentation.** Describes configuration/inheritance
behavior; runtime parentage/effective settings require actual preflight evidence.

All package-defined bounds, design decisions, architecture, acceptance targets,
role decomposition and tests are proposed requirements, not performance or
security guarantees asserted by these sources. Check current primary documentation
again when choosing actual dependency/toolchain versions at implementation time.
