# Reference-only Phase-1 material

Selected files from the user-supplied Phase-1 prompt archive are preserved exactly.
See `../sources/reference-provenance.json` for member names and hashes. They are
planning/context input and synthetic schema/examples, not evidence of the actual
implemented Phase-1 service. Their old phase exclusions/model instructions remain
historical text, not an instruction to execute Phase 1 during this Phase-2 task.

The executing agent uses current owner artifacts and contracts first. Links in
preserved reference documents may point to files in the original full archive;
they are not package-local missing deliverables. No Phase-1 implementation source
or proprietary game files are bundled here.
