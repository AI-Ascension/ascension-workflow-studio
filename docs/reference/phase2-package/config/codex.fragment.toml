# Inert example: merge only supported keys into a scoped config after preflight.
# Never overwrite global settings, permissions, or approval policy.
[agents]
default_subagent_model = "gpt-5.6-luna"
default_subagent_reasoning_effort = "max"
max_concurrent_threads_per_session = 12
# No max_depth key is asserted. Real recursion/depth support must be verified.
