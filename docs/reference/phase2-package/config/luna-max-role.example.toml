# Inert per-role model layer for an installed client that supports these fields.
model = "gpt-5.6-luna"
model_reasoning_effort = "max"
# Tool/sandbox permissions and role registration are inherited and verified;
# they are not broadened by this example.
